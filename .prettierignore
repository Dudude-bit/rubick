# Generated by the Tauri build; its formatting is the generator's, not ours.
# Reformatting them only guarantees the next build undoes it.
src-tauri/gen/

# Historical design docs and plans. They are a record of what was decided
# when, not living prose — reformatting one to change a word rewrites the
# whole file and buries the change.
docs/superpowers/

# Recordings of what a real controller wrote, regenerated by the ignored Rust
# dumper against the specimen cluster. Reformatting one would make the next
# regeneration look like a change.
src/lib/__fixtures__/
